# Optional — only needed if you want to preview the site locally.
# GitHub Pages builds the site for you without this.
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "jekyll-remote-theme"
gem "jekyll-seo-tag"
